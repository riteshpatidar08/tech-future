# Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Connection
# For production: Use your MongoDB Atlas connection string
# Example: mongodb+srv://username:password@cluster.mongodb.net/database?appName=Cluster0
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/syntaxim?appName=Cluster0

# JWT Secret Key
# IMPORTANT: Change this to a strong, random secret in production
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# Frontend URL for CORS
# For local development: http://localhost:8080 or http://localhost:5173
# For production: Your frontend domain (e.g., https://yourdomain.com)
FRONTEND_URL=http://localhost:8080
